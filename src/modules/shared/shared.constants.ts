export enum STATUS {
    ACTIVE = 1,
    INACTIVE = 0,
}

export enum PROTOCOL {
    SOLANA = 'solana',
    ETHEREUM = 'ethereum',
    TON = 'ton',
}

export enum NetworkType {
    MAINNET = 'mainnet',
    TESTNET = 'testnet',
}

export enum CHAIN_TYPE {
    EIP155 = 'EIP155',
    SOLANA = 'SOLANA',
    TRON = 'TRON',
    TON = 'TON',
}
